{{

# Note that this file is not used directly, but rather transformed into the real dist.ini

<<EOM
;;; this file was automatically generated from
;;; ${\ $dist->name }-${\ $dist->version }/t/corpus/file-from-code/build_template/dist.ini
EOM
}}
name    = Foo
version = 1.23
author  = foobar
license = Perl_5
copyright_holder = foobar
copyright_year   = 2009
abstract = Foo and a plugin

[GatherDir]

[Test::NewVersion]

; this is to test that we get the right version from the index -- this test
; will not pass if running immediately after releasing this module, as the index
; won't have updated yet.
[GenerateFile / file-from-code]
filename = lib/Dist/Zilla/Plugin/Test/NewVersion.pm
is_template = 0
content = package Dist::Zilla::Plugin::Test::NewVersion;
content = $VERSION = '{{ $dist->version }}';
content = 1;
